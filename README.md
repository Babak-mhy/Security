# Security

This repository contains a simple OSINT automation script `osint_tool.py`.

## Installation

Install Python dependencies:

```bash
pip install -r requirements.txt
```

## Usage

Run the tool with a target URL:

```bash
python osint_tool.py https://example.com
```

The script fetches the web page and prints out all hyperlink references found.
